version: 2
updates:
  # Keep GitHub Actions (workflows + local composite actions) up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
    labels:
      - area/dependencies
      - area/ci
